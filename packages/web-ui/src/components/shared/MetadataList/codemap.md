# MetadataList

## Responsibility

Vertical spacing container for metadata rows.

## Design

`React.memo` component. Renders children in a `space-y-2` container.

## Flow

Pure presentational. No state.

## Integration

Used by `TorrentDetailsOverviewSection` and `StatisticsScreenBody` for structured metadata display.
