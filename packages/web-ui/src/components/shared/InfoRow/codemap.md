# InfoRow

## Responsibility

Label + value display row for read-only metadata.

## Design

`React.memo` component. Renders uppercase label and breakable value text in a bordered card.

## Flow

Pure presentational. No state.

## Integration

Used by `RSSScreenBody` for feed metadata and detail screens for property display.
