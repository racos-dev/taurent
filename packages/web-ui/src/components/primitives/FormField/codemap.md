# FormField

## Responsibility

Label + description + error + children wrapper for consistent form field layout.

## Design

`React.memo` component. Renders label (optional), description (optional), children, and error message (optional) in a vertical stack with `space-y-1.5`.

## Flow

Pure presentational. No state.

## Integration

Used by form UIs in server setup and settings screens.
