# AuthLoadingScreen

## Responsibility

Full-screen centered loading spinner with customizable text for authentication and connection states.

## Design

`React.memo` component. Renders a `Spinner` (ring variant, large size) and a text label centered on the full viewport.

## Flow

Pure presentational. No state.

## Integration

Used by app shells during authentication and server connection establishment.
